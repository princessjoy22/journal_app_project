<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <div class="py-2 font-medium text-neutral">
      <%= form.label :name %>
    </div>
    <div><%= form.text_field :name, id: :category_name, class: "rounded h-10 w-52 pl-2 focus:outline-none focus:ring focus:border-red-800"%></div>
  </div>

  <div class="py-6">
    <div class="text-center text-white"><%= form.submit "Create", class: "w-52 h-10 rounded bg-neutral btn btn-outline" %></div>
  </div>
<% end %>
